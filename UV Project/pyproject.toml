[project]
name = "gsc"
version = "0.1.0"
description = "GSC dashboard and LoRa/plotting scripts for Torito"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.5",
    "numpy>=1.20",
    "pandas>=1.3",
    "pyserial>=3.5",
]
